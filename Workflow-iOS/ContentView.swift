import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("I am inside master branch. Check workflow try 3")
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
